"""gitpeek: a read-only, hg crecord-style terminal browser for a git commit.

The interface mirrors ``hg commit -i`` (the *crecord* selector): a
foldable tree with the same keybindings, the same indentation idiom, and
the same general look. There is no selection — you cannot stage or
unstage anything — only navigation and inspection.
"""

__version__ = "0.1.0"
